var gulp = require('gulp');
var pug = require('gulp-pug');

var paths = {
  src: './src/**',
  dist: './public/'
}

gulp.task('build', function () {
  gulp.src('')
});
gulp.task('default', ['build']);
